import type { ChatRoomItem } from '@/typescript/interface/chat.ts';

/* export const getDefaultChatRoomListData = (): ChatRoomItem => {
  return {
    currentPage: 0;
    empty: true;
    first: true;
    last: true;
    list: {
        applyType: '';
        chatRoomId: number;
        lastMessageContent: string;
        lastMessageRead: boolean;
        lastMessageTime: string;
        opponentId: number;
        opponentNickname: string;
        opponentProfileImgUri: string | null;
        projectId: number;
        projectTitle: string;
    }
  }
}; 
 */
